
spring:
  redis:
    database: 0
    host: 192.168.25.128
    password:
    pool:
      6379
    jedis:
      pool:
        max-active: 8
        min-idle: 0
        max-idle: 8
        max-wait: -1
  cache:
    redis:
      time-to-live:
